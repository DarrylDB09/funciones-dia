#Ejemplo para calcular el area el triangulo

# Variables de entradae
base = int(input("Ingrese la base: "))
altura = int(input("Ingrese la altura: "))

# proceso
def calcularAreaTriangulo(b,a):
    area = (b*a)/2
    return

#Invocar la funcion
resultado = calcularAreaTriangulo(base, altura)